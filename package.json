{
  "name": "togetherscreen",
  "version": "1.0.0",
  "private": true,
  "description": "A real-time watch-party web app and Chrome extension powered by Socket.IO.",
  "workspaces": [
    "web",
    "server",
    "extension"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "build:web": "npm run build --workspace web",
    "setup:extension": "npm run vendor --workspace extension",
    "build": "npm run build:web && npm run setup:extension",
    "check": "node --check server/index.js && node --check extension/background.js && node --check extension/content.js && node --check extension/popup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
